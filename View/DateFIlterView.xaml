<UserControl x:Class="ToDoList.View.DateFIlterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ToDoList.View"
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="150">
    <DockPanel>
        <ComboBox IsEditable="False">
            <DockPanel >
                <TextBlock Text="Дата:" DockPanel.Dock="Top"/>
                <DatePicker DockPanel.Dock="Bottom" IsTodayHighlighted="True" Focusable="True" />
            </DockPanel>
            <GridSplitter VerticalAlignment="Stretch" HorizontalAlignment="Stretch"/>
            <CheckBox Content="День"/>
            <CheckBox Content="Неделя"/>
            <CheckBox Content="Месяц"/>
        </ComboBox>
    </DockPanel>
</UserControl>
