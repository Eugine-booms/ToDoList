<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:models="clr-namespace:ToDoList.Model">
    <DataTemplate DataType="{x:Type models:ToDoTask}">
       
    </DataTemplate>
    <!--<DataTemplate DataType="{x:Type decanat:ToDoModel}">
        <TextBlock  Text="{Binding Name, StringFormat='группа {0}'}"/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type decanat:Student}">
        <TextBlock Text="{Binding Name, StringFormat='Студент {0}'}"/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type sys:String}">
        <TextBlock >
          <Run Foreground="Red" FontWeight="Bold">Строка:</Run> <TextBlock Text="{Binding}"/>
        </TextBlock>
    </DataTemplate>
    <DataTemplate DataType="{x:Type sys:Int32}">
        <TextBlock >
                                    <Run Foreground="Green" FontWeight="Bold">Число:</Run> <TextBlock Text="{Binding}"/>
                                </TextBlock>
        </DataTemplate>-->
</ResourceDictionary>